{
  "name": "shunsaku-monorepo",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.7.1",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "start": "turbo start",
    "lint": "turbo lint",
    "format": "turbo format",
    "lint:fix": "turbo lint:fix"
  },
  "devDependencies": {
    "turbo": "^2.3.5"
  }
}
